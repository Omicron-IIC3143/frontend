name: Linter Testing

# Enable Buildkit and let compose use it to speed up image building
env:
  DOCKER_BUILDKIT: 1
  COMPOSE_DOCKER_CLI_BUILD: 1

on:
  pull_request:
    branches: [ dev, adding-ci-file ]

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v3
      
      - name: npm Install
        run: npm install
        
      #- name: Create env file
      #  run: |
      #    touch .env
      #    echo REACT_APP_API_URL="http://social-starter-dev.herokuapp.com" >> .env
      #    cat .env
          
      #- name: npm Start
      #  run: npm start
      
      - name: Run linters
        uses: wearerequired/lint-action@v2
        with:
          eslint: true
      #    prettier: true
          
      # Source: https://dev.to/jnybgr/how-to-visualize-eslint-errors-on-github-3f8p
      #- name: Exec Linter Eslint
      #  run: npm run lint --output-file eslint_report.json --format json src
        
      #- name: Annotate Code Linting Results
      #  uses: ataylorme/eslint-annotate-action@1.0.4
      #  if: always()
      #  with:
      #    repo-token: '${{ secrets.GITHUB_TOKEN }}'
      #    report-json: 'eslint_report.json'


        
      
        

      #- name: Build instance
      #  run:  docker-compose -f docker-compose.yml build
        
      #- name: Run Linter
      #  run: docker-compose -f docker-compose.yml run --rm web npx eslint ./
        
      #- name: Tear down the Stack
      #  run:  docker-compose -f docker-compose.yml down
